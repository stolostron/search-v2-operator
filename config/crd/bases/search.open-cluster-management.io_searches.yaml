
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: searches.search.open-cluster-management.io
spec:
  group: search.open-cluster-management.io
  names:
    kind: Search
    listKind: SearchList
    plural: searches
    singular: search
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Search is the Schema for the searches API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SearchSpec defines the desired state of Search
            properties:
              allowDenyResources:
                description: Control list of Kubernetes resources indexed by search-collector
                properties:
                  allowedResources:
                    description: Allowed resources from collector
                    items:
                      properties:
                        apiGroups:
                          description: API Group names to be filtered
                          items:
                            type: string
                          type: array
                        clusterLabels:
                          description: Cluster Labels this filter to be applied
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: A label selector requirement is a selector
                                  that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: operator represents a key's relationship
                                      to a set of values. Valid operators are In,
                                      NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: values is an array of string values.
                                      If the operator is In or NotIn, the values array
                                      must be non-empty. If the operator is Exists
                                      or DoesNotExist, the values array must be empty.
                                      This array is replaced during a strategic merge
                                      patch.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: matchLabels is a map of {key,value} pairs.
                                A single {key,value} in the matchLabels map is equivalent
                                to an element of matchExpressions, whose key field
                                is "key", the operator is "In", and the values array
                                contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                        resources:
                          description: Resource names to be filtered
                          items:
                            type: string
                          type: array
                      required:
                      - apiGroups
                      - resources
                      type: object
                    type: array
                  deniedResources:
                    description: Denied resources from collector
                    items:
                      properties:
                        apiGroups:
                          description: API Group names to be filtered
                          items:
                            type: string
                          type: array
                        clusterLabels:
                          description: Cluster Labels this filter to be applied
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: A label selector requirement is a selector
                                  that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: operator represents a key's relationship
                                      to a set of values. Valid operators are In,
                                      NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: values is an array of string values.
                                      If the operator is In or NotIn, the values array
                                      must be non-empty. If the operator is Exists
                                      or DoesNotExist, the values array must be empty.
                                      This array is replaced during a strategic merge
                                      patch.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: matchLabels is a map of {key,value} pairs.
                                A single {key,value} in the matchLabels map is equivalent
                                to an element of matchExpressions, whose key field
                                is "key", the operator is "In", and the values array
                                contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                        resources:
                          description: Resource names to be filtered
                          items:
                            type: string
                          type: array
                      required:
                      - apiGroups
                      - resources
                      type: object
                    type: array
                type: object
              availabilityConfig:
                description: 'Specifies deployment replication for improved availability.
                  Options are: Basic and High (default)'
                type: string
              customDbConfig:
                description: Kubernetes secret name containing user provided db secret
                  Secret contains connection url, certificates
                type: string
              dbConfig:
                description: Configmap name contains parameters to override default
                  db parameters
                type: string
              dbStorage:
                description: Storgeclass Name to be used by default db
                properties:
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: storage capacity
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: name of the storage class
                    type: string
                type: object
              deployments:
                description: Customization for search deployments
                properties:
                  api:
                    description: Configuration for api Deployment
                    properties:
                      imageOverride:
                        description: Image_override
                        type: string
                      imagePullPolicy:
                        description: ImagePullPolicy
                        type: string
                      imagePullSecret:
                        description: ImagePullSecret
                        type: string
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: NodeSelector to schedule on nodes with matching
                          labels
                        type: object
                      proxyConfig:
                        additionalProperties:
                          type: string
                        description: Proxy config , if remote collectors need to override
                        type: object
                      replicaCount:
                        description: Number of pod instances for deployment
                        type: integer
                      resources:
                        description: Compute Resources required by deployment
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Limits describes the maximum amount of compute
                              resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Requests describes the minimum amount of
                              compute resources required. If Requests is omitted for
                              a container, it defaults to Limits if that is explicitly
                              specified, otherwise to an implementation-defined value.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                        type: object
                    type: object
                  collector:
                    description: Configuration for collector Deployment
                    properties:
                      imageOverride:
                        description: Image_override
                        type: string
                      imagePullPolicy:
                        description: ImagePullPolicy
                        type: string
                      imagePullSecret:
                        description: ImagePullSecret
                        type: string
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: NodeSelector to schedule on nodes with matching
                          labels
                        type: object
                      proxyConfig:
                        additionalProperties:
                          type: string
                        description: Proxy config , if remote collectors need to override
                        type: object
                      replicaCount:
                        description: Number of pod instances for deployment
                        type: integer
                      resources:
                        description: Compute Resources required by deployment
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Limits describes the maximum amount of compute
                              resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Requests describes the minimum amount of
                              compute resources required. If Requests is omitted for
                              a container, it defaults to Limits if that is explicitly
                              specified, otherwise to an implementation-defined value.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                        type: object
                    type: object
                  database:
                    description: Configuration for DB Deployment
                    properties:
                      imageOverride:
                        description: Image_override
                        type: string
                      imagePullPolicy:
                        description: ImagePullPolicy
                        type: string
                      imagePullSecret:
                        description: ImagePullSecret
                        type: string
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: NodeSelector to schedule on nodes with matching
                          labels
                        type: object
                      proxyConfig:
                        additionalProperties:
                          type: string
                        description: Proxy config , if remote collectors need to override
                        type: object
                      replicaCount:
                        description: Number of pod instances for deployment
                        type: integer
                      resources:
                        description: Compute Resources required by deployment
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Limits describes the maximum amount of compute
                              resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Requests describes the minimum amount of
                              compute resources required. If Requests is omitted for
                              a container, it defaults to Limits if that is explicitly
                              specified, otherwise to an implementation-defined value.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                        type: object
                    type: object
                  indexer:
                    description: Configuration for indexer Deployment
                    properties:
                      imageOverride:
                        description: Image_override
                        type: string
                      imagePullPolicy:
                        description: ImagePullPolicy
                        type: string
                      imagePullSecret:
                        description: ImagePullSecret
                        type: string
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: NodeSelector to schedule on nodes with matching
                          labels
                        type: object
                      proxyConfig:
                        additionalProperties:
                          type: string
                        description: Proxy config , if remote collectors need to override
                        type: object
                      replicaCount:
                        description: Number of pod instances for deployment
                        type: integer
                      resources:
                        description: Compute Resources required by deployment
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Limits describes the maximum amount of compute
                              resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Requests describes the minimum amount of
                              compute resources required. If Requests is omitted for
                              a container, it defaults to Limits if that is explicitly
                              specified, otherwise to an implementation-defined value.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                        type: object
                    type: object
                  remoteCollector:
                    description: Configuration for addon installed collector Deployment
                    properties:
                      imageOverride:
                        description: Image_override
                        type: string
                      imagePullPolicy:
                        description: ImagePullPolicy
                        type: string
                      imagePullSecret:
                        description: ImagePullSecret
                        type: string
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: NodeSelector to schedule on nodes with matching
                          labels
                        type: object
                      proxyConfig:
                        additionalProperties:
                          type: string
                        description: Proxy config , if remote collectors need to override
                        type: object
                      replicaCount:
                        description: Number of pod instances for deployment
                        type: integer
                      resources:
                        description: Compute Resources required by deployment
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Limits describes the maximum amount of compute
                              resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Requests describes the minimum amount of
                              compute resources required. If Requests is omitted for
                              a container, it defaults to Limits if that is explicitly
                              specified, otherwise to an implementation-defined value.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                            type: object
                        type: object
                    type: object
                type: object
            type: object
          status:
            description: SearchStatus defines the observed state of Search
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned
                      format: date-time
                      type: string
                    message:
                      description: Human readable message
                      type: string
                    reason:
                      description: Reason for the condition's last transition.
                      type: string
                    status:
                      type: string
                    type:
                      description: SearchConditionType
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              db:
                description: Database used by search
                type: string
              health:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file Human readable health state'
                type: string
              storageInUse:
                description: Storage used by database
                type: string
            required:
            - db
            - health
            - storageInUse
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
