apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: collectionconfigs.search.open-cluster-management.io
spec:
  group: search.open-cluster-management.io
  names:
    kind: CollectionConfig
    listKind: CollectionConfigList
    plural: collectionconfigs
    shortNames:
    - sccc
    singular: collectionconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: collection-config is the schema for the collection-configs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: collection-config spec defines the configuration changes
              made to the resources and fields indexed by Search Collectors
            properties:
              collectNamespaces:
                description: Specified the namespaces where resources are to be indexed
                  by Search Collectors
                properties:
                  namespaceSelector:
                    properties:
                      exclude:
                        description: List of namespace names to exclude
                        items:
                          type: string
                        type: array
                      include:
                        description: List of namespace names to include
                        items:
                          type: string
                        type: array
                      matchExpressions:
                        description: Label selector expressions for matching namespaces
                        items:
                          properties:
                            key:
                              type: string
                            operator:
                              type: string
                            values:
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: Label selector for matching namespaces
                        type: object
                    type: object
                type: object
              collectionRules:
                description: Defines a list of rules for collecting resources and
                  specific fields, as proposed in the Alternatives section.
                items:
                  properties:
                    action:
                      description: Defines whether to Include (collect) or Exclude
                        (deny collection) resources matching the selector.
                      enum:
                      - Include
                      - Exclude
                      type: string
                    collectAdditionalPrinterColumnsPriority:
                      description: Specifies the priority on additionalPrinterColumns
                        for which to collect.
                      type: integer
                    collectAnnotations:
                      description: Specifies to collect annotations for the resource.
                      type: boolean
                    collectConditions:
                      description: Specifies to collect status conditions for the
                        resource.
                      type: boolean
                    fields:
                      description: Specifies the additional fields on the resource
                        to index by Search Collectors.
                      items:
                        properties:
                          displayName:
                            type: string
                          jsonPath:
                            type: string
                          name:
                            description: Specified the name of the collected item
                              on the resource.
                            type: string
                          type:
                            description: Data type of resource field to be indexed
                              by Search Collectors. Default is a string.
                            enum:
                            - DataTypeBytes
                            - DataTypeSlice
                            - DataTypeString
                            - DataTypeNumber
                            - DataTypeMapString
                            type: string
                        required:
                        - name
                        - jsonPath
                        type: object
                      type: array
                    resourceSelector:
                      description: Specifies which resources this rule applies to.
                      properties:
                        apiGroups:
                          description: Specifies apiGroups of resources.
                          items:
                            type: string
                          type: array
                        kinds:
                          description: Specifies kinds of resources.
                          items:
                            type: string
                          type: array
                      required:
                      - apiGroups
                      - kinds
                      type: object
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
